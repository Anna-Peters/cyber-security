Linux CLI:
1. grep "search keyword" file name
grep -E "value1|value2|value3|" file name
2. wc -l file name for the amount of entries in the file
3. find -name filename and find -name *.fileformat
4. & to run the command in the background
5. && to chain the commands, coomand1 && command2, command2 will be only executed if command1 is successfull
6. echo "textvalue > filename - replace the txt in the file
7. echo "textvalue" >> filename - add text to the file 
8. ssh accountusername@IP -> enter the password to connect to the remote machine
9. touch name of the file - to create a file
10. mkdir to create directory
11. rm filename or rm -R directory name to remove file or directory
12. mv filename1 filename2 - rename the filename1 and merge the content filename2
13. file filename - to know the file extension
14. su account name
15. ls -lh to check permisssions 
r = 4
w = 2
e = 1 
777 = full access for owner, group and others

Active Directory
mstsc /v:ip address, to access remote machine after the delegation done
GPO's for a spicific policies can be extracted in cmd to see the full list. Can be placed under the domain or under organization units.
PC-01 -> PC-01$ machine name 

CLI Windows:

1. driverquery | more show page by page results
2. systeminfo, ver for details 
3. ssh user_name@ip_address
4. ping 
5. netstat - abon
6. nslookup
7. tracert
8. dir, mdir, rmdir
dir /a - Displays hidden and system files as well.
dir /s - Displays files in the current directory and all subdirectories.
9. cd for the current directory
10. copy *.md C:\Markdown
11. type, more use with Spacebar
12. erase, del
13. chkdsk: checks the file system and disk volumes for errors and bad sectors.
14. driverquery: displays a list of installed device drivers.
15. sfc /scannow: scans system files for corruption and repairs them if possible.

Powershell
Get-Command -CommandType "Function" 
Get-help name_of_cmdlets
Get-alias for Windowns CLIc\ commands

Cmdlet -Property "pattern*" - Find-Module -Name "PowerShell*" 
Install-Module -Name "PowerShellGet"
ls/dir - GetChildItem
cd - Set-Location -Path ".\path_name"
Create a new item or fodler: New-Item -Path ".\captain-cabin\captain-wardrobe\file_name.txt" -ItemType "Directory" or -ItemType "file

Copy-Item -Path .\captain-cabin\file_name_1.txt -Destination .\captain-cabin\file_name_2.txt
type/cat - Get-Content -Path ".\file_name"

Sort-bject 
Get-ChildItem | Sort-Object Length -Descending | Select-Object -First 1
Get-ChildItem | Select-Object Name,Length 
Get-ChildItem | Where-Object -Property "Name" -like "ship*"

grep/findstr - Select-String -Path ".\captain-hat.txt" -Pattern "hat"
Where-Object filters the files by their Extension property, ensuring that only files with extension equal (-eq) to .txt are listed.
Get-ChildItem | Where-Object -Property "Name" -like "ship*" 
PS C:\Users\captain\Documents\captain-cabin> Get-ChildItem | Where-Object -Property "Name" -like "ship*"  
Get-ChildItem | Where-Object -Property "Extension" -eq ".txt"

The operator -eq (i.e. "equal to") is part of a set of comparison operators that are shared with other scripting languages (e.g. Bash, Python). To show the potentiality of the PowerShell's filtering, we have selected some of the most useful operators from that list:

-ne: "not equal". This operator can be used to exclude objects from the results based on specified criteria.
-gt: "greater than". This operator will filter only objects which exceed a specified value. It is important to note that this is a strict comparison, meaning that objects that are equal to the specified value will be excluded from the results.
-ge: "greater than or equal to". This is the non-strict version of the previous operator. A combination of -gt and -eq.
-lt: "less than". Like its counterpart, "greater than", this is a strict operator. It will include only objects which are strictly below a certain value.
-le: "less than or equal to". Just like its counterpart -ge, this is the non-strict version of the previous operator. A combination of -lt and -eq.

Get-NetTCPConnection displays current TCP connections, giving insights into both local and remote endpoints. This cmdlet is particularly handy during an incident response or malware analysis task, as it can uncover hidden backdoors or established connections towards an attacker-controlled server.
Scripting:
For blue team professionals such as incident responders, malware analysts, and threat hunters, PowerShell scripts can automate many different tasks, including log analysis, detecting anomalies, and extracting indicators of compromise (IOCs). These scripts can also be used to reverse-engineer malicious code (malware) or automate the scanning of systems for signs of intrusion.

For the red team, including penetration testers and ethical hackers, PowerShell scripts can automate tasks like system enumeration, executing remote commands, and crafting obfuscated scripts to bypass defences. Its deep integration with all types of systems makes it a powerful tool for simulating attacks and testing systems’ resilience against real-world threats.
get-help invole-command -examples

